# Running locally

One offs:

- `rbenv local 3.4.5`
- `bundle install`

Build and run:

- `bundle exec jekyll build`
- `bundle exec jekyll serve --livereload`

Then open: `http://127.0.0.1:4000/dev/`.
